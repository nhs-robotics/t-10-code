package t10.gamepad.input;

/**
 * Represents a pressable input to the gamepad.
 */
public interface GIsPressed {
	/**
	 * Returns whether this is pressed or not.
	 *
	 * @return Pressed (true) or not pressed (false).
	 */
	boolean isPressed();
}
